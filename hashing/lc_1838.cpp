#include <bits/stdc++.h>
using namespace std;

//1838. Frequency of the Most Frequent Element

int solution(vector<int>v){

}

int main() {
    int n;
    cin>>n;
    vector<int>v(n);
    for (int i = 0; i < n; i++)
    {
        cin>>v[i];
    }

    

    return 0;
}